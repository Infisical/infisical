{{- if and .Values.gateway.enrollment.method .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "infisical-gateway.fullname" . }}-credentials
  labels:
    {{- include "infisical-gateway.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.persistence.accessModes | nindent 4 }}
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.size | quote }}
{{- end }}
---
{{- if and .Values.persistence.enabled (not .Values.persistence.sessionRecording.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "infisical-gateway.fullname" . }}-session-recordings
  labels:
    {{- include "infisical-gateway.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.persistence.sessionRecording.accessModes | nindent 4 }}
  {{- if .Values.persistence.sessionRecording.storageClass }}
  storageClassName: {{ .Values.persistence.sessionRecording.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.sessionRecording.size | quote }}
{{- end }}
