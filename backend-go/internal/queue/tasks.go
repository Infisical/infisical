package queue

// Task type names are defined here as constants.
// Each task has a corresponding typed Task[P] definition that enforces payload type safety.
const (
	TaskNameAuditLog = "audit:log"
)
