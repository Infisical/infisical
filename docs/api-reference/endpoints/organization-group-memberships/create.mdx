---
title: "Create organization group membership"
openapi: "POST /api/v1/organizations/memberships/groups/{groupId}"
---

Creates an organization group membership (links a group to the current org). Sub-organization only: links a parent-org group;
