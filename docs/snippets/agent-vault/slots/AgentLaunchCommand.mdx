import { AgentVaultBranch } from "/snippets/AgentVaultQuickstartConfigurator.jsx";

<AgentVaultBranch agent="none">
  For example, with Claude:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- claude
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="claude-code">
  For Claude Code, the command is `claude`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- claude
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="codex">
  For Codex, the command is `codex`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- codex
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="cursor">
  For Cursor, the command is `cursor-agent`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- cursor-agent
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="hermes">
  For Hermes Agent, the command is `hermes`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- hermes
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="openclaw">
  For OpenClaw, the command is `openclaw`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- openclaw
  ```
</AgentVaultBranch>

<AgentVaultBranch agent="opencode">
  For OpenCode, the command is `opencode`:

  ```bash
  infisical agent-vault run --session-token <session-token> --proxy <proxy-host>:17323 -- opencode
  ```
</AgentVaultBranch>
