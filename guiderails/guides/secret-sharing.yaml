# Two independent <Steps> blocks in one file (share a secret, request a secret), which is
# why the extractor treats procedures rather than files as the unit of work.
guide: docs/documentation/platform/secret-sharing.mdx
fixture: org-with-project
tab: null
critical: false
requiresLicense: false
skipSteps: []
watch:
  - frontend/src/pages/organization/SecretSharingPage/**
  - frontend/src/pages/public/ShareSecretPage/**
  - frontend/src/hooks/api/secretSharing/**
  - backend/src/services/secret-sharing/**
notes: >
  Skip the "Emails (optional)" and "Allow external recipients" fields: both send mail, and
  the stack under test has no SMTP configured. The Custom Branding section further down the
  page is license-gated and sits outside both procedures, so it is not walked.
