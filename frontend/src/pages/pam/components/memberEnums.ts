export enum PamMemberKind {
  User = "user",
  Group = "group",
  Identity = "identity"
}

export enum PamMemberSource {
  Direct = "direct",
  Inherited = "inherited"
}

export enum PamMembershipScope {
  Account = "account",
  Folder = "folder"
}
