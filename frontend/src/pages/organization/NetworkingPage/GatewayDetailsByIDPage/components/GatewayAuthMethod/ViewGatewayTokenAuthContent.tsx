// Token auth has no configurable fields — the "Method: Token" detail row in the parent
// Authentication card is the entire visible config. Keep the dispatcher tidy by rendering
// nothing here.
export const ViewGatewayTokenAuthContent = () => null;
