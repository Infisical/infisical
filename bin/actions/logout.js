const netrc = require('netrc-rw');
const {
	LOGIN_HOST,
	KEYS_HOST
} = require('../variables');

/**
 * Clear stored credentials
*/
const logout = () => {
	
	try {
		
	} catch (err) {

	}

	console.log('TODO');
	process.exit(0);
}

module.exports = logout;
