export enum PamResource {
  Postgres = "postgres",
  MySQL = "mysql",
  SSH = "ssh",
  Kubernetes = "kubernetes",
  AwsIam = "aws-iam",
  Redis = "redis"
}

export enum PamResourceOrderBy {
  Name = "name"
}
