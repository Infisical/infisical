export enum HoneyTokenType {
  AWS = "aws"
}

export enum HoneyTokenStatus {
  Active = "active",
  Triggered = "triggered",
  Revoked = "revoked"
}

export enum HoneyTokenEventType {
  AWS = "aws"
}
