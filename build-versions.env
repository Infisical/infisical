# Single source of truth for versions pinned across multiple Dockerfiles.
# Read by the Makefile for compose builds and by CI for image builds.
# Dockerfiles declare the ARG without a default, so a build that does not pass
# it fails loudly rather than silently installing an unpinned version.
INFISICAL_CLI_VERSION=0.43.128
